## android リファクタ A-S4: 主体別音声キャッシュと主体離脱の事後条件（ADR-104 決定 1〜3・5〜9・14・16、SG-A6・SG-C17）

## 概要
音声キャッシュを主体（`user_id`）ごとのディレクトリに分け、起動時に他主体の残骸を回収し、ダウンロード 3 操作の実体を `podcast/OfflineLibrary.kt`（主体固定・起動時回収・cancel を所有する capsule。SG-C17）へ移して `PodcastViewModel` を中継だけにし、ダウンロードジョブの主体を開始時に固定し、主体離脱を認証状態の遷移から導出して「トークン破棄 → 未認証または次の主体 → 後始末（完了を待たない）」の順に改め、logout は破棄前に捕捉したトークンで `POST /auth/logout` を送り（await しない）、client からの FCM 登録解除呼出を削除してサーバ側の連鎖削除に任せ、主体依存の端末設定 4 key を消す。共有仕様 §4.4 SL-06 / SL-07 の Android 側を green にし、SL-01 / SL-02 / SL-04 の完全な事後条件を満たす。正本は [ADR-104](../../../../docs/adr/104-subject-departure-and-subject-scoped-assets.md)（決定 1〜3・5〜10・14・16・追記 SG-A1/SG-A6。2026-09-23 user 判断 Q10/Q11/Q13 で確定した補足を含む）、共有仕様 `docs/design/shared-playback-spec.md` §4.4 SL-01〜SL-07・§6.3（性質 1〜5・Android 行）・§6.5（遷移 ①②④・事後条件表・分類表）、親 docs `docs/design/android-design.md` §7.2「主体離脱の検知」「主体離脱の後始末」「主体別の端末ローカル資産」・§7.3（A-S4 行）、Implementation Spec §3.2（主体が離れる事後条件・`CleanupIncomplete`）・§4 CI-T12・CI-T13。**検証モード（再設計しない）**。generate_spec の spec.md は CI-T12・CI-T13 の抜粋＋SL-01〜SL-07 で足り、新しい契約 ID を作らない。

## 前提・着手条件
- 依存 slice（android 内）: **A-S1**（`BaseFakeApiClient`。`logout` の署名変更を 9 Fake へ波及させないため）の android PR が main に merge 済み **かつ** 親リポ `news-listen` の submodule ポインタが進んでいる（親で `git submodule status` を実行し `android` 行に `+` が無い）。A-S2a〜A-S2c・A-S3 の merge は前提にしない（下記「後始末の手順集合」参照）。
- 依存契約（backend。PR 番号ではなく契約の存在で判定する）: 親 main の `backend` submodule で **(a)** `GET /auth/me` と `POST /auth/login`（passkey login を含む）の応答に `user_id`（`[A-Za-z0-9_-]`）が載っている（ADR-104 決定 15。`backend/docs` の API 仕様と契約テストで確認）、**(b)** セッション削除（`POST /auth/logout`・失効・期限切れ掃除）で FCM の device-token 行が連鎖削除される（決定 10。`unregisterDeviceToken` の client 呼出を消しても登録が残らない根拠）。両方が親 main に入っていることを着手前に確認し、無ければ投入しない（B-S5 の完了と親ポインタの前進で満たされる）。
- baseline: 着手時点の全 unit テストが green。
- 確定済みの判断（再提案しない）: 待機案（後始末の完了を待ってから遷移）は ADR-104 で棄却。離脱時の端末単位の全削除（主体を持たない `removeAll`）は決定 2 で不採用。平置きキャッシュの移行はしない（決定 7）。認証の猶予受理はしない（決定 17）。「8 key は消さない」は SG-A6 で撤回（Spec §5 rejected_overdesign「preferences 消去契約の追加」はこの撤回で上書き）。消す主体依存 key は **4 key**（`default_difficulty`・`default_playback_speed`・`weekly_goal_episodes`・`seen_achievement_ids`。分類表の Android 列「（未実装）」は誤りで router が訂正済み）。client からの FCM 登録解除呼出は削除し B-S5 の連鎖削除（決定 10）に任せる（Q11）。`Unknown` の間は起動時回収をしない（Q10。共有仕様 §6.3 性質 2 に追記）。
- **SG-C17**（2026-09-24 user 判断）: `download` / `removeDownload` / `cancelDownloadsAndClearCache` の実体を `PodcastViewModel` から **`podcast/OfflineLibrary.kt`**（新設 capsule。主体固定・起動時回収・cancel を所有）へ移し、`PodcastViewModel` は `download(podcast)` / `removeDownload(id)` と `downloadingIds` / `downloadedIds` の**中継だけ**を持つ。`AudioCacheManager` を直接触るのは `OfflineLibrary` だけにする（完了条件の grep）。**学習の中継 3 操作**（`loadVocabularyRegistrations` / `saveVocabulary` / `submitQuizAnswers`）は `PodcastViewModel` に**残す**（根拠: 親 docs §7.2「`PodcastViewModel` に残る責務」が「語彙登録・クイズ中継の 3 操作は `ApiClient` 経由のまま残す（RF8 保留: 解除条件 = 学習機能のサイクル）」と定める。本 slice の契約 ID に学習は無い）。
- **SG-C16**（同日）: 主体離脱の後始末に位置同期を含めない。本 slice の手順集合に「位置同期」を足さない（`stopForSubjectLeave` は A-S2b2 が「位置同期なし」で作る）。
- 2026-09-24 に確定した小決定（導出値。再提案しない）: (i) 後始末の手順 `CleanupStep(name: String, run: suspend (Subject) -> Unit)` の `name` は **`playback_stop` / `download_cancel` / `audio_cache` / `preferences` / `fcm_local_reset`** の 5 値（この順に実行。`playback_stop` は A-S2b2 の `stopForSubjectLeave` が merge 済みのときだけ存在）。`CleanupIncomplete(parts: Set<String>)` の `parts` はこの値の部分集合。(ii) `Subject` = `data class Subject(val userId: String?, val username: String)`（`auth/AuthState.kt` に置く。`userId` は `user_id` を `[A-Za-z0-9_-]` で検証した値、不正・欠落は `null`）。`AuthViewModel` への関数注入の名前と型は **`onSubjectLeave: (Subject) -> Unit`**（非 suspend。lock の外で 1 回呼び、戻りを待たない）。手順列の実行体は **`auth/SubjectCleanup.kt`**（`steps: List<CleanupStep>`・`scope`・`cleanupIncomplete: StateFlow<CleanupIncomplete?>`・`fun start(subject: Subject)`）で、`AppContainer` が `onSubjectLeave = subjectCleanup::start` を注入する。Spec §3.2 の「`AuthViewModel` の StateFlow」は「auth 層（`SubjectCleanup`）の StateFlow」と読み替え、`AuthViewModelTest` は `SubjectCleanup` を実物で組んで観測する（docs 反映で Spec の文言を揃える）。(iii) `logoutsInProgress` ガードは**残す**（確定値。順序変更後も `onUnauthorized` の二重発火抑止は `logoutsInProgress > 0` と `attachedToken != sessionStore.load()` の両方で行う）。(iv) SL-04 の「再実行」の入口は 2 つだけ: ①同じ主体で再ログインしたあとの**明示 logout の再実行**（手順列がもう一度走り、全手順成功で `cleanupIncomplete` が `null` に戻る）、②**次回起動の回収**（`reclaim` が離脱主体のディレクトリを消す）。`retryCleanup()` のような専用 API は作らない。(v) 起動時回収の関数注入は `onSubjectResolved: (Subject?) -> Unit`（`AuthViewModel` が `Unknown` から `Authenticated` / `Unauthenticated` に確定した直後と遷移 ④ で B が確立した直後に 1 回呼ぶ。`AppContainer` は `{ s -> offlineLibrary.reclaim(s?.userId) }` を注入）。(vi) `invalidate(subject, id)` に渡す主体は**セッション開始時に固定した値**（`PodcastViewModel` が再生開始時に `currentSubject()` を 1 回捕捉して保持。A-S2b2 側も `currentSubject()` を都度呼ばず、session が保持する主体を渡す）。
- 2026-09-24 の実測（本 slice が変える現状）: `AuthViewModel.onUnauthorized` は後始末を呼ばない（`AuthViewModel.kt:232`「order:12」）。`logout()` は `onSubjectLeave()` の完了後に `sessionStore.clear()`（`:342-350`。テスト `AuthViewModelTest.kt:704` が pin）。`onSubjectLeave: suspend () -> Unit`（`:65`）、`logoutsInProgress`（`:120`）。`CleanupIncomplete` は未実装（grep 0）。`AudioCacheManager` は `{baseDir}/audio/{id}.mp3` の平置き（`removeAll()` は `AudioCacheManager.kt:81`。公開操作 `cache` / `isCached` / `cachedFileUri` / `remove` / `removeAll` / `cacheSize`）。`AudioCacheManager` を触る main のコードは `PodcastViewModel.kt` の 6 呼出（`:177, 209, 235, 248, 293, 299`）と `AppContainer.kt:111` の生成だけ。`UserResponse` に `user_id` 無し。`_authState.value =` の書込は `AuthViewModel.kt` に 8 箇所。`FcmTokenRegistrar.onLogout()`（`:43-47`）が `unregisterDeviceToken` を呼ぶ。**grep 内訳**（`grep -rn 'cancelDownloadsAndClearCache\|removeAll()' app/src/main`）= **14 行**: `cancelDownloadsAndClearCache` **10 行**（`AppContainer.kt:257, 259, 269, 274`・`AuthViewModel.kt:49, 55`・`PodcastViewModel.kt:105, 108, 187, 232`）＋ `removeAll()` で加わる **4 行**（`AppContainer.kt:256`・`AudioCacheManager.kt:81`・`FileSystem.kt:8`・`PodcastViewModel.kt:235`。`AppContainer.kt:259` は両方を含む）。内訳: コード 4 行（`AppContainer.kt:274` 呼出・`PodcastViewModel.kt:232` 宣言・`:235` 呼出・`AudioCacheManager.kt:81` 宣言）＋コメント 10 行。コメントのうち `PodcastViewModel.kt` の 3 行（`:105, 108, 187`）は `OfflineLibrary` への移設で消え、**書き換えるコメントは 7 箇所**（`AppContainer.kt:256, 257, 259, 269`・`AuthViewModel.kt:49, 55`・`FileSystem.kt:8`）。`code == 401` は 2 件。`PodcastViewModelTest.kt` のダウンロード節は `:856-1054`（199 行・10 件。`cancelDownloadsAndClearCache` は `:1009, :1038`）、コンストラクタ呼出は helper 1 箇所。
- 正本間の差の扱い（決定ではなく採用先の明記）: 親 docs `android-design.md` §7.2「主体離脱の後始末」と親 plan の A-S4 行は「主体依存の **3 key**」「FCM トークン解除」と書くが、共有仕様 §6.5 の分類表（Android 列に `seen_achievement_ids` を含む **4 key**）と §6.5 Android 行（FCM は連鎖削除に任せ client 呼出を A-S4 で削除。SG-B4）が 2026-09-23 の後の改訂で、本 order はこちらを採る。§7.2 側の文言は完了時の docs 反映で揃える。Spec §4 CI-T12 の「cleanup 3 手順（音声・FCM・再生停止）」の FCM は「ローカルリセットのみ」と読み替える。
- `docs/trial-log/`（android `auth-401-concurrent-refresh-race.md`・親）を最初に読む。

## 対象（android サブモジュールのみ。ファイル単位。main 13 ファイル = 既存 11 ＋ 新設 `podcast/OfflineLibrary.kt`・`auth/SubjectCleanup.kt`。test 12 ファイル = 既存 10 ＋ 新設 `OfflineLibraryTest`・`SubjectCleanupTest`）
**変更（main）**
1. `model/UserResponse.kt`: `@SerialName("user_id") val userId: String? = null`（決定 16: 任意。欠落でも decode 成功）。
2. `network/AudioCacheManager.kt`: 保存先を `{baseDir}/audio/{user_id}/{podcast_id}.mp3` へ。公開操作はすべて主体を受ける: `cache(subject, id, bytes)` / `isCached(subject, id)` / `cachedFileUri(subject, id)` / `remove(subject, id)` / `cachedIds(subject)` / `removeSubject(subject)` / `reclaim(currentSubject: String?)`（`audio/` 直下の通常ファイル＝旧平置きを全削除し、`currentSubject` 以外のディレクトリを削除。`null` は全ディレクトリ削除）/ `cacheSize(subject)`。`subject` の形式検証は `validateId` と同じ `[A-Za-z0-9_-]`（決定 6）で、不正なら `InvalidId` と同型の例外。A-S2b1 が merge 済みなら `invalidate(subject, id)` も同じ形へ揃える。`removeAll()` は削除する。呼び手は下記 4a の `OfflineLibrary` だけ。
3. `network/FileSystem.kt`・`JavaFileSystem.kt`: `listDirectories(dirPath)` と `deleteRecursively(path)` を追加（`FakeFileSystem` にも同じ実装）。
4a. **`podcast/OfflineLibrary.kt`（新設。SG-C17）**: `class OfflineLibrary(cacheManager: AudioCacheManager, podcastApi: PodcastApi, currentSubject: () -> String?, dispatcher: CoroutineDispatcher)`。`PodcastViewModel.kt:95-116`（`downloadJobs` と `_downloadingIds` / `_downloadedIds`）と `:170-253`（`syncDownloadedState` / `download` / `performDownload` / `cancelDownloadsAndClearCache` / `removeDownload`）の実体を移す。公開面: `downloadingIds` / `downloadedIds: StateFlow<Set<String>>`、`suspend fun download(podcast)`（**開始時に `currentSubject()` を 1 回捕捉**して `performDownload` に渡し、書込は捕捉した主体のディレクトリへ。決定 9。主体が `null`（未認証・`user_id` 欠落）なら何もしない＝`downloadingIds` に入らない）、`suspend fun removeDownload(id)`（呼出時の `currentSubject()`）、`fun syncDownloadedState(podcasts)`（呼出時の `currentSubject()`。`null` なら空集合）、`fun isCached(subject: String?, id)` / `fun cachedFileUri(subject: String?, id)` / `fun invalidate(subject: String?, id)`（`subject == null` は false / null / no-op。決定 16: キャッシュ無効で動作継続）、`suspend fun cancelDownloads(subject: String)`（その主体で開始した進行中 Job を `cancel()`。**`join` しない**。`downloadingIds` / `downloadedIds` を空にする）、`fun removeSubject(subject: String)`（`cacheManager.removeSubject`）、`fun reclaim(currentSubject: String?)`（`cacheManager.reclaim`）。離脱後に完了した書込は離脱主体のディレクトリに入る（SL-06）。`auth/` を import しない（主体は `String?` で受ける）。エラーは従来どおり `errorMessage` に反映するため `lastError: StateFlow<String?>` を持ち、`PodcastViewModel` が `errorMessage` へ合流する（文言不変）。
4b. `podcast/PodcastViewModel.kt`: コンストラクタの `cacheManager: AudioCacheManager` を `offlineLibrary: OfflineLibrary` に替え、`currentSubject: () -> String?` を注入（`tokenProvider` と同じ関数注入）。`download(podcast)` / `removeDownload(id)` は `offlineLibrary` への 1 行中継、`downloadingIds` / `downloadedIds` は `offlineLibrary` のものを再公開、`fetchPodcasts` 後の同期は `offlineLibrary.syncDownloadedState(podcasts)`。`cancelDownloadsAndClearCache()` は**削除**（呼び手は `AppContainer` の手順列から `offlineLibrary` を直接呼ぶ）。再生の CACHED 判定（`:293, 299`）は**再生開始時に `currentSubject()` を 1 回捕捉した値**で `offlineLibrary.isCached(subject, id)` / `cachedFileUri(subject, id)` を呼ぶ（小決定 (vi)。A-S2b2 が先なら捕捉地点は `startEpisode` になっており、本 slice は `invalidate(subject, id)` の 2 引数化だけ追随する）。学習の中継 3 操作は触らない。
5. `auth/AuthViewModel.kt`: (a) `_authState` への書込を 1 箇所（`sessionLock` 内の遷移関数）に集約し、遷移 ①`Authenticated(A)`→`Unauthenticated`（logout）・②同（`onUnauthorized`）・④`Authenticated(A)`→`Authenticated(B)`（`login` / `completePasskeyLogin`。`A.userId != B.userId`、`userId` が片方でも null なら `username` 比較）で離脱主体 A を導出し、lock の外で後始末を `internalScope` に起動する（**完了を待たない**）。⑤`Authenticated`→`Unknown` は離脱でない（現行どおり `Unknown` へ落とさない）。`applyProfileUpdate` は同一主体の更新で離脱でない。(b) `logout()` の順序を「破棄前に `sessionStore.load()` でトークンを捕捉」→ `sessionStore.clear()` → `Unauthenticated` → 後始末（① で起動）→ `apiClient.logout(token)`（捕捉したトークンで既存の `Authorization: Bearer` を付けて `POST /auth/logout`。新ヘッダは作らない。`internalScope` で起動し **await しない**。best-effort・独立 try/catch）に改める。(c) `onSubjectLeave: suspend () -> Unit` を **`onSubjectLeave: (Subject) -> Unit`** に変え、遷移 ①②④ で導出した離脱主体 `Subject(userId, username)` を渡して lock の外で 1 回呼ぶ（戻りを待たない）。手順列の実行は新設 **`auth/SubjectCleanup.kt`**: `class SubjectCleanup(steps: List<CleanupStep>, scope: CoroutineScope)`、`fun start(subject: Subject)` は `scope.launch` で各手順を**この順**（`playback_stop` → `download_cancel` → `audio_cache` → `preferences` → `fcm_local_reset`）に独立 try/catch で実行し、失敗した手順名の集合を `cleanupIncomplete: StateFlow<CleanupIncomplete?>`（`parts: Set<String>`）に置く。全手順成功なら `null` に戻す。手順は冪等（再実行の入口は小決定 (iv) の 2 つ）。`AuthState.kt` に `Subject` と `CleanupStep` / `CleanupIncomplete` を置く。(d) `logoutsInProgress` ガードは**残す**（確定値。`onUnauthorized` は `logoutsInProgress > 0` または `attachedToken != sessionStore.load()` なら無視する）。(e) `onSubjectResolved: (Subject?) -> Unit` を追加し、`Unknown` → `Authenticated` / `Unauthenticated` の確定直後と遷移 ④ の B 確立直後に 1 回呼ぶ（起動時回収の入口。`Unknown` の間は呼ばない）。
6. `network/ApiClient.kt`・`OkHttpApiClient.kt`: `logout(token: String)` にし、渡されたトークンで既存の `Authorization: Bearer <token>` ヘッダを要求に付与する（決定 14。新しいヘッダ名は作らない。`AuthInterceptor` は `tokenProvider` が null のとき既存ヘッダに触れないため、破棄後でも届く。この要求の 401 は `attachedToken == null` なので `onUnauthorized` を発火しない）。
7. `preferences/PreferencesStore.kt`・`DataStorePreferencesStore.kt`・`InMemoryPreferencesStore.kt`: `clearSubjectScoped()` を追加し `default_difficulty`・`default_playback_speed`・`weekly_goal_episodes`・`seen_achievement_ids` の 4 key を削除する（共有仕様 §6.5 分類表・SG-A6）。`article_open_mode`・`time_format`・`sfx_enabled`・`haptics_enabled` は残す。
8. `di/AppContainer.kt`: `OfflineLibrary` を 1 インスタンス生成し（`AudioCacheManager` を渡すのはここだけ）、`PodcastViewModel` には `offlineLibrary` と `currentSubject = { (_authViewModel.authState.value as? Authenticated)?.user?.userId?.takeIf(validateId) }` を注入。`SubjectCleanup` を組み、`AuthViewModel` に `onSubjectLeave = subjectCleanup::start` と `onSubjectResolved = { s -> offlineLibrary.reclaim(s?.userId) }` を注入。**手順列 = 着手時点の `onSubjectLeave` に含まれる手順を名前付きで引き継ぐ**（2026-09-24 時点: `download_cancel` = `{ s -> s.userId?.let { offlineLibrary.cancelDownloads(it) } }`・`audio_cache` = `{ s -> s.userId?.let { offlineLibrary.removeSubject(it) } }`（旧 `cancelDownloadsAndClearCache` を 2 手順に分ける）・`fcm_local_reset` = `{ fcmTokenRegistrar.onLogout() }`）＋ 本 slice で足す `preferences` = `{ preferencesStore.clearSubjectScoped() }`。**A-S2b2 が先に merge されている場合の追随**（A-S2b2 側の同名節と対称）: 既存ラムダの `_podcastViewModel.stopForSubjectLeave()` を `CleanupStep("playback_stop")` として**先頭**に置く（5 手順）。A-S2b2 が未 merge なら 4 手順で、`playback_stop` は A-S2b2 が足す（許可）。起動時の回収は `onSubjectResolved` 経由で `AuthViewModel` の起動時解決が `Authenticated`（`user_id` あり・形式正）/ `Unauthenticated` / `Authenticated` だが `user_id` 欠落・形式不正（`null` 扱い＝全削除）に確定した直後に走る（遷移 ④ で B が確立したときも同じ）。`Unknown`（SL-03: トークン保持・判定保留）の間は回収しない（Q10 確定・共有仕様 §6.3 性質 2）。コメント 4 箇所（`:256, 257, 259, 269`）を新名へ書き換える。
9. `notification/FcmTokenRegistrar.kt`: `onLogout()` から `apiClient.unregisterDeviceToken(token)` の呼出と `fetchTokenOrNull()` を削除し、`isAuthenticated = false` のリセットだけ残す（Q11 = A。サーバ側の登録解除は B-S5 の連鎖削除＝決定 10）。手順集合には残す。

**変更（test）**: `auth/AuthViewModelTest.kt`（順序 pin `:704`「後始末の完了後に遷移」と失効経路の非呼出 pin `:106, :562`「order:12」を仕様変更として反転。logout 中の失効通知 `:648, :674`「完了まで無視」は順序変更後の観測点へ書き換える＝破棄後に届く失効通知は `attachedToken != sessionStore.load()` で無視され後始末が二重に走らないこと。`:757`（logout の cancel）は「`sessionStore.clear()` の前で cancel されれば何も起きず、後なら `Unauthenticated` と後始末起動が済んでいる（中間状態なし）」へ書き換える。SL-01・SL-02・SL-04・SL-07 を追加。既存の SL-03（`CI-T10` の一時障害 3 件）・SL-05（`CI-T21`）のテスト名に行 ID を付す）、`network/AudioCacheManagerTest.kt`（14 件を主体別パスへ書き換え・`reclaim`・平置き削除）、**`podcast/OfflineLibraryTest.kt`（新設）**: `PodcastViewModelTest.kt:856-1054` のダウンロード節 10 件（`:859, 879, 900, 915, 935, 952, 963, 986, 1009, 1038`）を移し、`:1009, :1038` の `cancelDownloadsAndClearCache` 2 件は `cancelDownloads(subject)` ＋ `removeSubject(subject)` の主体固定・不 join へ反転（SL-06）、`download` は主体 `null` で no-op（決定 16）を追加。`podcast/PodcastViewModelTest.kt`（ダウンロード節を削除し、`download` / `removeDownload` が `OfflineLibrary` へ中継されることと `downloadedIds` の再公開を各 1 件で観測。helper のコンストラクタを `offlineLibrary` へ）、**`auth/SubjectCleanupTest.kt`（新設）**: 手順順序・独立 try/catch・`cleanupIncomplete` の `parts`・全成功で `null`（CI-T13 の実行体側）、`network/OkHttpApiClientTest.kt`（`logout(token)` のヘッダ）、`preferences/*PreferencesStoreTest.kt`（`clearSubjectScoped` 4 key）、`notification/FcmTokenRegistrarTest.kt`（`onLogout` が API を呼ばないことへ反転）、`model/AuthDecodingTest.kt`（`user_id` 有無）、`network/FakeFileSystem.kt`。

## 契約（CI → T / SL の対応）
| 契約 | 内容 | 検証 |
|---|---|---|
| SL-01 | 明示 logout: トークン破棄 → `Unauthenticated`（後始末の完了を待たない）→ 離脱主体のディレクトリが空・4 key 無し（再生停止は A-S2b2 の手順が集合にあればその呼出も観測） | `AuthViewModelTest`（後始末を `delay` させる double で `Unauthenticated` が先に観測される） |
| SL-02 | 保存トークン付き API の 401 → SL-01 と同じ事後条件 | 同上（`onUnauthorized(attachedToken)`） |
| SL-04 / CI-T13 | 手順の 1 つが失敗しても残りは実行され `CleanupIncomplete(parts)`（`parts` ⊂ 5 値）が観測可能。再実行（入口 ①: 同じ主体で再ログイン → 明示 logout。入口 ②: 次回起動の `reclaim`）で同じ事後条件、全成功で `cleanupIncomplete == null` | `AuthViewModelTest`（`SubjectCleanup` を実物で組む）＋`SubjectCleanupTest`＋`AudioCacheManagerTest`（入口 ②） |
| SL-06 / 決定 9 | 離脱時に cancel が間に合わず書込が完了する double → 書込は A のディレクトリ → 次回起動の `reclaim(B or null)` で A のディレクトリが消える | `OfflineLibraryTest`＋`AudioCacheManagerTest` |
| SG-C17 | `download` / `removeDownload` は `PodcastViewModel` から `OfflineLibrary` へ中継され、`AudioCacheManager` を直接触るのは `OfflineLibrary` だけ | `PodcastViewModelTest`（中継 2 件）＋完了条件の grep |
| SL-07 / 決定 3 ④ | `Authenticated(A)` → `login(B)` で A の後始末が走り、B の確立は待たない | `AuthViewModelTest` |
| CI-T12 | 失効通知後・logout 後の事後条件（手順集合の全呼出） | T-T12（既存を拡張） |
| 決定 7・8 | `reclaim`: `audio/*.mp3` の平置きを全削除、`currentSubject` 以外のディレクトリ削除、`null` で全削除 | `AudioCacheManagerTest`（表駆動） |
| 決定 14 | `logout(token)` が `Authorization: Bearer <token>` を送り、破棄後に送っても届く。`AuthViewModel.logout` は送信完了を待たずに `Unauthenticated` へ遷移する | `OkHttpApiClientTest`（MockWebServer）＋`AuthViewModelTest`（`logout` を遅延させる Fake で遷移が先） |
| 決定 10 / Q11 | `FcmTokenRegistrar.onLogout()` は API を呼ばず `isAuthenticated` だけ落とす（以後 `onNewToken` で登録しない） | `FcmTokenRegistrarTest` |
| 決定 16 | `user_id` 欠落で decode 成功・`download` 無効・`isCached` false・起動回収は全削除 | `AuthDecodingTest`＋`OfflineLibraryTest` |

## 特性テスト（baseline。着手前に green）
`auth/AuthViewModelTest`（45）、`network/AudioCacheManagerTest`（14）、`podcast/PodcastViewModelTest`（52。A-S2b1 / A-S2b2 が先に merge されていればその追加分を含む）、`network/OkHttpApiClientTest`（47）、`notification/FcmTokenRegistrarTest`（12）、`preferences/DataStorePreferencesStoreTest`（5）・`InMemoryPreferencesStoreTest`（6）、`model/AuthDecodingTest`（2）。件数は 2026-09-23 実測。

## 手順
1. baseline green を記録。
2. `UserResponse.userId` → `AuthDecodingTest` → GREEN。
3. `FileSystem` 拡張 → `AudioCacheManager` 主体別化（決定 7・8・16 の表駆動）→ RED → GREEN。
4. `OfflineLibrary` 新設（ダウンロード節 10 件を `OfflineLibraryTest` へ移し、主体固定・不 join・主体 `null` で no-op を追加。SL-06・決定 16）→ RED → GREEN → `PodcastViewModel` を中継化（中継 2 件）→ GREEN。
5. `PreferencesStore.clearSubjectScoped`（4 key）→ RED → GREEN。
5b. `FcmTokenRegistrar.onLogout` の API 呼出削除 → `FcmTokenRegistrarTest` 反転 → GREEN。
6. `ApiClient.logout(token)`（決定 14）→ RED → GREEN。
7. `SubjectCleanup`（`SubjectCleanupTest`: 順序・独立 try/catch・`parts`）→ RED → GREEN → `AuthViewModel` の遷移導出・順序・`onSubjectLeave: (Subject) -> Unit`・`onSubjectResolved`（SL-01・02・04・07、CI-T12・T13）→ 反転対象の既存テストを先に反転 → RED → GREEN。
8. `AppContainer` の配線（`OfflineLibrary`・手順列・`currentSubject`・起動時回収）。
9. 2 PR（下記「規模」の分割）。temporary path なし。

## 完了条件
- `JAVA_HOME=<JBR> ./gradlew clean testDebugUnitTest` 全 green。
- SL-01・SL-02・SL-03・SL-04・SL-05・SL-06・SL-07 がテスト名に含まれ、T-T12・T-T13 が `verifies: CI-T12` / `CI-T13` を持つ。
- **参照 0 件**（対象集合 = `app/src/main` の全 Kotlin。コメントも含む。除外 = `docs/`・`build/`・`app/src/test`）: `grep -rn 'removeAll()\|cancelDownloadsAndClearCache' app/src/main` = 0。2026-09-24 実測の分母 = **14 行**（`cancelDownloadsAndClearCache` 10 行 ＋ `removeAll()` で加わる 4 行。内訳は上記「実測」）。書き換えるコメントは 7 箇所（`AppContainer.kt:256, 257, 259, 269`・`AuthViewModel.kt:49, 55`・`FileSystem.kt:8`）、`PodcastViewModel.kt` の 3 箇所は移設で消える。`grep -rn '"audio"' app/src/main` の一致は `network/AudioCacheManager.kt` のみ。`grep -rn '_authState.value =' app/src/main/java/com/rioikeda/newslisten/auth/AuthViewModel.kt` = 1（遷移関数）。
- **`AudioCacheManager` を直接触るのは `OfflineLibrary` だけ**（SG-C17）: `grep -rn 'cacheManager\.\|audioCacheManager\.' app/src/main` の一致はすべて `podcast/OfflineLibrary.kt`（2026-09-24 実測の分母 = `PodcastViewModel.kt` 6 呼出 ＋ `AppContainer.kt` コメント 1）。`grep -rln 'AudioCacheManager' app/src/main` = `network/AudioCacheManager.kt`（宣言）・`network/FileSystem.kt`（コメント）・`podcast/OfflineLibrary.kt`・`di/AppContainer.kt`（生成 1 箇所とコメント）の 4 ファイルで、`podcast/PodcastViewModel.kt` を含まない。`grep -n 'fun download\|fun removeDownload' app/src/main/java/com/rioikeda/newslisten/podcast/PodcastViewModel.kt` = 2（中継の宣言のみ。本体は各 1 行）。`grep -rn 'invalidate(' app/src/main` の一致は `network/AudioCacheManager.kt` 宣言・`podcast/OfflineLibrary.kt` 中継の 2 箇所（A-S2b2 が merge 済みなら `podcast/PodcastViewModel.kt` の呼出を加えた 3 箇所。A-S2b2 の完了条件「2 箇所」を本 slice が 3 箇所へ更新する（許可済み））。
- 手順列の名前: `grep -rn 'CleanupStep(' app/src/main/java/com/rioikeda/newslisten/di/AppContainer.kt` の `name` は `download_cancel` / `audio_cache` / `preferences` / `fcm_local_reset`（A-S2b2 merge 済みなら先頭に `playback_stop`）だけで、この順。`grep -rn 'onSubjectLeave' app/src/main/java/com/rioikeda/newslisten/auth/AuthViewModel.kt` の宣言型が `(Subject) -> Unit`。
- `grep -rn 'fun logout' app/src/main/java/com/rioikeda/newslisten/network` の署名がすべて `logout(token: String)`。`grep -rn 'unregisterDeviceToken' app/src/main` の一致は `network/ApiClient.kt`・`OkHttpApiClient.kt` の宣言／実装のみ（呼出 0）。
- `clearSubjectScoped` が消す key が `KEY_DEFAULT_DIFFICULTY`・`KEY_DEFAULT_PLAYBACK_SPEED`・`KEY_WEEKLY_GOAL_EPISODES`・`KEY_SEEN_ACHIEVEMENT_IDS` の 4 つと一致（`DataStorePreferencesStoreTest` で全 8 key を書いてから呼び、残る 4 key を検査）。
- 反転した既存テスト（`AuthViewModelTest.kt:106, 562, 704, 648, 674, 757`・`PodcastViewModelTest.kt:1009, 1038` → `OfflineLibraryTest` へ移して反転）が PR 本文に「テスト名 → 決定番号 / 行 ID」で列挙されている。
- 後始末の手順集合が PR 本文に `CleanupStep.name` の順で列挙されている（着手時点の集合＋`preferences`。FCM 手順はローカルリセットのみ）。
- grep oracle 回帰なし: `error("` = 0。`grep -rn 'code == 401' app/src/main | wc -l` = **2**（件数判定。2026-09-24 実測: `network/AuthInterceptor.kt:48`・`network/OkHttpApiClient.kt:459`。`logout(token)` の 401 を新たに比較しない）。`currentPodcast` の件数は本 slice で変えない。
- レビュー観点: `AuthInterceptor` が `auth/` を import しない。`PodcastViewModel`・`OfflineLibrary` が `auth/` を import しない（主体は関数注入・`String?`）。`SubjectCleanup` が `podcast/` / `network/` / `preferences/` を import しない（手順は `AppContainer` が組む）。`core/` を触っていない。学習の中継 3 操作（`loadVocabularyRegistrations` / `saveVocabulary` / `submitQuizAnswers`）の本体が不変（diff に現れない）。

## 禁止事項 / scope 外
- 後始末の完了を待ってから遷移する実装にしない（ADR-104 で棄却）。`cancelAndJoin` に戻さない。
- 主体を持たない全削除（`removeAll`）を残さない。平置きキャッシュを主体ディレクトリへ移行しない。
- client から `unregisterDeviceToken` を呼ぶ経路を残さない（server 側の登録解除は B-S5 決定 10 の連鎖）。`registerDeviceToken`（認証時の再登録）は変えない。
- 消す端末設定は 4 key に限る。`article_open_mode`・`time_format`・`sfx_enabled`・`haptics_enabled` は消さない。
- `user_id` を必須にしない（欠落でログイン不能にしない。決定 16 は Q13 で 3 platform 共通）。`Unknown` の間に回収を走らせない。logout 用の新しいヘッダ名を作らない。admin 一覧に `user_id` を求めない（決定 15）。
- `PlaybackSession` / `nowPlaying`（A-S2a〜c）・CI（A-S3）を触らない。仕様にない業務条件を足さない。
- 学習の中継 3 操作を `OfflineLibrary` にも他の capsule にも移さない（学習サイクルまで保留）。後始末の手順に位置同期を足さない（SG-C16）。`retryCleanup()` 等の再実行 API を作らない（入口は小決定 (iv) の 2 つ）。

## 規模（見込み行数。根拠 = 2026-09-24 実測）
- **production ≈ 650 行**（追加 ≈ 450・削除 ≈ 200。`UserResponse.kt` +1、`AudioCacheManager.kt` 99 行 → 主体別化 +45/−15、`FileSystem.kt` +6・`JavaFileSystem.kt` +12・`FakeFileSystem.kt` +15、`OfflineLibrary.kt` 新規 ≈ 130（`PodcastViewModel.kt:95-116` の 22 行と `:170-253` の 84 行 = 106 行を移し主体を足す）、`PodcastViewModel.kt` −106/+20（中継）、`AuthState.kt` +25（`Subject` / `CleanupStep` / `CleanupIncomplete`）、`SubjectCleanup.kt` 新規 ≈ 60、`AuthViewModel.kt` 413 行 → +90/−45（遷移関数 1 箇所への集約・logout 順序・④ 導出・`onSubjectResolved`）、`ApiClient.kt` / `OkHttpApiClient.kt` +6/−3、`PreferencesStore` 3 ファイル +25、`AppContainer.kt` +45/−25、`FcmTokenRegistrar.kt` −10）。
- **test ≈ 1,050 行**（`AuthViewModelTest.kt` 893 行 → 反転 6 件 + SL-01/02/04/07・T-T12/T-T13 追加 ≈ +230/−50、`SubjectCleanupTest.kt` 新規 ≈ 80、`AudioCacheManagerTest.kt` 141 行・14 件 → 主体別へ全件書き換え + `reclaim` 表駆動 ≈ +130/−70、`OfflineLibraryTest.kt` 新規 ≈ 260（移す 199 行 + 主体固定 / `null` no-op）、`PodcastViewModelTest.kt` −199/+40（中継 2 件・helper）、`OkHttpApiClientTest.kt` +25、`PreferencesStoreTest` 2 ファイル +35、`FcmTokenRegistrarTest.kt` +10/−25、`AuthDecodingTest.kt` +15）。
- **合計 ≈ 1,700 行**で 1,000 行を超えるため **2 PR に分けて投入する**（order は 1 つ。takt のタスクは PR ごと。両 PR とも本書の「前提・着手条件」を満たす）:
  - **A-S4a（資産の主体化）**: 対象 1〜4b・8 のうち `OfflineLibrary` 生成と `currentSubject` 注入・`onSubjectResolved` の追加（対象 5 の (e) だけ）と起動時回収。契約 = 決定 7・8・9・16・SL-06・SG-C17。既存の `onSubjectLeave` ラムダは「`offlineLibrary.cancelDownloads(subject)` → `removeSubject(subject)`」に置き換えるだけで順序（後始末 → 破棄）は変えない。≈ 900 行。
  - **A-S4b（離脱の導出と順序）**: 対象 5（(a)〜(d)）・6・7・9・8 の手順列と `SubjectCleanup`。契約 = SL-01・02・04・07・CI-T12・T13・決定 3・10・14・SG-A6。反転する `AuthViewModelTest` 6 件はこちら。≈ 800 行。
  - 分割理由: 前者は「資産に主体を持たせる」境界、後者は「離脱を遷移から導出し順序を変える」境界で、A-S4a 単独でも SL-06 が green になり、失敗時の巻き戻しが独立する。完了条件の grep は A-S4a で `removeAll()` / `cancelDownloadsAndClearCache` / `AudioCacheManager` 直接参照、A-S4b で `_authState.value =` / `fun logout` / `unregisterDeviceToken` / `CleanupStep(` を判定する。

## 検証
- `JAVA_HOME=<JBR> ./gradlew clean testDebugUnitTest --console=plain` → exit 0。
- 上記 grep 8 本の結果を PR 本文に貼る。

## 記録
- 反転したテスト・手順集合・`user_id` 欠落時の観測を PR 本文と `android/docs/trial-log/` に残す。
- 親 docs への返却事項: 共有仕様 §4.4 SL-06 / SL-07 と SL-01 / SL-02 / SL-04 の Android 側の保留を解除できる旨（§5 の解除条件）。`design/android-design.md` §7.2「主体離脱の検知」「主体離脱の後始末」「主体別の端末ローカル資産」を現状記述へ。**SG-C17** により §7.2「`PodcastViewModel` に残る責務」の操作 15 から `cancelDownloadsAndClearCache` を外し `download` / `removeDownload` を中継と明記、`OfflineLibrary` の置き場（`podcast/OfflineLibrary.kt`）を §2 の表へ。Spec §3.2 の `CleanupIncomplete` の置き場を `SubjectCleanup` へ。

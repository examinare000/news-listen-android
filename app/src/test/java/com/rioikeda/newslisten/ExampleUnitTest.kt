package com.rioikeda.newslisten

import org.junit.Test
import org.junit.Assert.*

/**
 * Placeholder unit test.
 *
 * This test will be replaced with table-driven test cases that validate
 * the shared playback state machine according to docs/design/shared-playback-spec.md
 * (located in the parent news-listen repository's docs submodule).
 */
class ExampleUnitTest {
    @Test
    fun addition_isCorrect() {
        assertEquals(4, 2 + 2)
    }
}

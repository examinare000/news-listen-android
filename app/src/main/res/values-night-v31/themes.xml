<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Dark mode API 31+: Theme.NewsListen.Starting は values-night に集約済み
         本体テーマのみ定義。window chrome 属性の enforceNavigationBarContrast を API 31+ で false に固定。 -->
    <style name="Theme.NewsListen" parent="android:Theme.Material.Light.NoActionBar">
        <item name="android:windowActionBar">false</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
        <item name="android:windowLightStatusBar">false</item>
        <item name="android:enforceNavigationBarContrast">false</item>
    </style>
</resources>
